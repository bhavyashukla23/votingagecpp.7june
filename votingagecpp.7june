#include<iostream>
using namespace std;
	
	int main()
	{
		int age;
		cout<<"Enter your age:";
		cin>>age;
		while(age>0)
		{
		  if(age>=18)
		   {
		     cout<<"You are eligible to vote";
		      break;
		   }
		   else
		    {
			 cout<<"You are under age and not eligible to vote";
			 break;
		    }
	    }
		
		return 0;
	}
